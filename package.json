{
  "name": "gpt-sidekick",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json && node scripts/build-extension.mjs"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.276",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
